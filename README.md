# Chain

Chain for Swift.

## Example
```
    lazy var createRoomButton: UIButton = {
        UIButton(type: .custom)
            .x
            .backgroundColor(##)
            .corners(radius: 30)
            .setTitleColor(##, for: .normal)
            .setTitle("", for: .normal)
            .instance
    }()
```
